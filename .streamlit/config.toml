# Theme is owned by app.py (st.session_state["theme"] + injected CSS) so
# users can toggle dark/light at runtime. Setting a [theme] block here
# would override our injected CSS via Streamlit's runtime variables.

[server]
headless = true
